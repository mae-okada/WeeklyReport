{
  "name": "weekly-report-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Weekly report generator dependencies",
  "scripts": {
    "install:deps": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt",
    "run": "python main.py"
  },
  "dependencies": {
    "pandas": "^2.2.4",
    "openpyxl": "^3.1.0",
    "deep-translator": "^1.0.0"
  }
}
